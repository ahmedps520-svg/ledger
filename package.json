{
  "name": "the-ledger",
  "version": "2.0.0",
  "description": "Subscription ledger for friends — admin dashboard plus a private dues link for each friend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "icons": "node scripts/generate-icons.js",
    "set-password": "node scripts/set-admin-password.js",
    "test": "node test/smoke.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true,
  "dependencies": {
    "pg": "^8.13.0"
  }
}
