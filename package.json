{
  "name": "the-ledger",
  "version": "1.0.0",
  "description": "Subscription ledger for friends — admin dashboard + friend self-serve portal, synced across devices.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
